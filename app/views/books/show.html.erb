<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<div>
  <nav class="page-nav">
    <%= link_to t('.link_to_edit'), edit_book_path(@book) %> |
    <%= link_to t('.link_to_index'), books_path %>
    <%= button_to t('.button_to_destroy'), @book, method: :delete %>
  </nav>
</div>
